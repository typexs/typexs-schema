import {
  find, filter, has, get, set, capitalize, isString,
  isFunction, isArray, isEmpty, clone, remove, isNull,
  snakeCase, merge, isNumber, isObject, isBoolean, isUndefined,
  map, defaults, first, last, assign, isSet, orderBy, uniq, concat
} from 'lodash';

export {
  find, filter, has, get, set, capitalize, isString,
  isFunction, isArray, isEmpty, clone, remove, isNull,
  snakeCase, merge, isNumber, isObject, isBoolean, isUndefined,
  map, defaults, first, last, assign, isSet, orderBy, uniq, concat
};

/*
export class LoDash {

  static find(...args: any[]) {
    return find.bind(null, args);
  }

  static filter(...args: any[]) {
    return filter.bind(null, args);
  }

  static has(...args: any[]) {
    return has.bind(null, args);
  }

  static set(...args: any[]) {
    return set.bind(null, args);
  }

  static get(...args: any[]) {
    return get.bind(null, args);
  }

  static capitalize(...args: any[]) {
    return capitalize.bind(null, args);
  }

  static isString(...args: any[]) {
    return isString.bind(null, args);
  }

  static isFunction(...args: any[]) {
    return isFunction.bind(null, args);
  }

  static isArray(...args: any[]) {
    return isArray.bind(null, args);
  }

  static isEmpty(...args: any[]) {
    return isEmpty.bind(null, args);
  }

  static isNull(...args: any[]) {
    return isNull.bind(null, args);
  }

  static clone(...args: any[]) {
    return clone.bind(null, args);
  }

  static remove(...args: any[]) {
    return remove.bind(null, args);
  }
}
*/
